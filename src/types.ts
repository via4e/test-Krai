export type playground = {
    x: number,
    y: number,
    bottomY: number,
    rightX: number,
}

export type sprite = {
    x: number,
    y: number,
    width: number,
    height: number,
    bottomY: number,
    rightX: number,
    mouseOn: boolean,
    controlled: boolean
}